// how to empty an array in js

// or can be asked like this

//do not reset it to a new array and do not loop through to pop each value


var arr = [1, 2, 3, 4, 5]

arr.length = 0;
console.log(arr);

// simple question
